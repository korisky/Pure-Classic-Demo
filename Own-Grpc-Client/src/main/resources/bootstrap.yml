server:
  port: 10102
spring:
  application:
    name: grpc-client
  profiles:
    active: local
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848
        namespace: local
        group: DEFAULT_GROUP
grpc:
  client:
    GLOBAL:
      negotiation-type: plaintext